env000_bootstrap:

  pre_commands:
    - 'spack bootstrap add --trust local-sources {{DEPLOY_SPACK_USER_CACHE_PATH}}/bootstrap_mirror/metadata/sources'
    - 'spack bootstrap add --trust local-binaries {{DEPLOY_SPACK_USER_CACHE_PATH}}/bootstrap_mirror/metadata/binaries'

