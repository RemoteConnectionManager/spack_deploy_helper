env000_bootstrap:

  pre_commands:
    - 'spack --no-env  bootstrap add --trust local-sources {{DEPLOY_SPACK_USER_CACHE_PATH}}/bootstrap_mirror/metadata/sources'
    - 'spack --no-env  bootstrap add --trust local-binaries {{DEPLOY_SPACK_USER_CACHE_PATH}}/bootstrap_mirror/metadata/binaries'

