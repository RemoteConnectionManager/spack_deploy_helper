
conda:
  substitutions:
    init_command: 'spack-python {{DEPLOY_ROOTPATH}}/scripts/select_spec.py --loglevel debug --external openssl --tplfile null_tpl.yaml'



  spack:
    definitions:
    - openssl:
      - '{{base_env.config.system_openssl}}'

    - conda:
      - conda

    - all:
      - $openssl
      - $conda

env_010_conda: '@REF{conda}'

