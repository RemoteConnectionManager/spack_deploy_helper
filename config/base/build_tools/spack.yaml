spack:
  definitions:
  - selected_openssl:
    - ${OPENSSL_SPEC}
  - tools:
    - cmake
    - git
    - perl
    - autoconf
    - automake
    - m4
    - patchelf

  - all:
    - $selected_openssl
    - $tools
  - system_compiler:
    - '%${COMPILER_SPEC}'
  specs:
  - matrix:
    - - $all
    - - $system_compiler
  view: false
  concretization: together
